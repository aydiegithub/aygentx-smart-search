name: Deploy AygentX to AWS Lambda via SAM

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    permissions:
      id-token: write  # Require for OIDC
      contents: read   # Allows actions/checkout to read code

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies and generate Requirements.txt
        run: |
          curl -LsSf https://astral.sh/uv/install.sh | sh
          uv export --no-hashes > requirements.txt

      - name: Setup AWS SAM CLI
        uses: aws-actions/setup-sam@v2

      # You can use AWS OIDC GitHub Actions module or normal AWS Access Keys.
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }} # e.g. us-east-1

      - name: SAM Build
        run: sam build --use-container

      - name: SAM Deploy
        env:
          API_SECRET_KEY: ${{ secrets.API_SECRET_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          CLOUDFLARE_DATABASE_ID: ${{ secrets.CLOUDFLARE_DATABASE_ID }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
        run: |
          sam deploy \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --stack-name aygentx-smart-search \
            --resolve-s3 \
            --capabilities CAPABILITY_IAM \
            --parameter-overrides \
                ApiSecretKey="${API_SECRET_KEY:-NotSet}" \
                GeminiApiKey="${GEMINI_API_KEY:-NotSet}" \
                OpenaiApiKey="${OPENAI_API_KEY:-NotSet}" \
                CloudflareDatabaseId="${CLOUDFLARE_DATABASE_ID:-NotSet}" \
                CloudflareAccountId="${CLOUDFLARE_ACCOUNT_ID:-NotSet}" \
                CloudflareApiToken="${CLOUDFLARE_API_TOKEN:-NotSet}"
